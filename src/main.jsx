import React from "react";
import { createRoot } from "react-dom/client";
import SplitSheet from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SplitSheet />
  </React.StrictMode>
);
